<Window x:Class="MainProject.ChangeFoodInventory_Parham.ChangeFoodINverntory.ChangeFoodInverntory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MainProject.ChangeFoodInventory_Parham.ChangeFoodINverntory"
        mc:Ignorable="d"
        Background="LightSkyBlue"
        Title="ChangeFoodInverntory" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.ColumnSpan="3" Grid.Row="0" Grid.Column="0" Text="Change Food inventory" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="30"/>
        <DataGrid x:Name="NamesButton" Grid.Row="1" Grid.RowSpan="2" Grid.ColumnSpan="2" Grid.Column="0" Background="LightSkyBlue" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Foods" Width="250">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button  Content="{Binding Name}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="inventory" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding RemNumber}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Change" Click="Change_Food_Inventory_Button"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Image Grid.Column="2" Grid.Row="0" Margin="-100,0,0,0" Source="/ChangeFoodInventory_Parham/Images/menu-symbol.jpg" Stretch="Fill" Width="40" Height="40"/>
        <Image Grid.Column="0" Grid.Row="0" Margin="100,0,0,0" Source="/ChangeFoodInventory_Parham/Images/menu-symbol.jpg" Stretch="Fill" Width="40" Height="40"/>
    </Grid>
</Window>
